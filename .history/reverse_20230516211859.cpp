#include <iostream>
#include <vector>
using namespace std;
int main()
{
    int arr[10] = {'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'};
    for (int i = 0; i < s.size() / 2; i++)
    {
        swap(s[i], s[s.size() - 1]);
    }
    return 0;
}
