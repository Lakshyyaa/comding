#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
using namespace std;
int main()
{
    vector<int> v = {1,6, -2, 3, 4, -5};
    vector<int>lol;
    for(int i=0;i<v.size();i++){
        if(v[i]>=0){
            
        }
    }
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << " ";
    }
    // cout<<a<<" "<<b;
    return 0;
}