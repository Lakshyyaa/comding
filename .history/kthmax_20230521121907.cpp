#include <iostream>
#include <vector>
#include <limits.h>
using namespace std;
int main()
{
    vector<int> nums = {6, 2, 5, 4, 1, 3, 0};
    int k = 2;
    vector<int>v;
    k = k % nums.size();
    int max=-1;
    for(int i=0;i<nums.size();i++){
        if(nums[i>]>max){
            
        }
        v.push_back(v[i]);
    }
    return 0;
}